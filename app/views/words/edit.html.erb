<h1>Editing Word</h1>

<%if !@word.nil?%>

<%= render 'form', word: @word %>

<%= link_to 'Show', @word %> | <%= link_to 'Destroy', @word, method: :delete, data: { confirm: 'Are you sure?' } %> |

<%else%>
This word belongs to another user
<%end%>
<%= link_to 'Back', words_path %>
